<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ELKKETA">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".DisplayCodeActivity" />
        <activity android:name=".CoursesActivity" />
        <activity android:name=".LessonsActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".UploadExamActivity" />
        <activity android:name=".ManageStudentsActivity" />
        <activity android:name=".ManageSubjectsActivity" />
        <activity android:name=".ManageCodesActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".CurriculumMapActivity" />
        <activity android:name=".MistakesActivity" />
        <activity android:name=".ActivatedCodesActivity" />
        <activity android:name=".ManageLecturesActivity" />
        <activity android:name=".ManageClassesActivity" />
        <activity android:name=".CodeActivationActivity" />
        <activity android:name=".AdminGateActivity" />
        <activity android:name=".SystemConfigActivity" />
        <activity android:name=".MainActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>